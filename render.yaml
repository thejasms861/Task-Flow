# render.yaml — Render Infrastructure-as-Code
# Docs: https://render.com/docs/blueprint-spec

services:
  # ─── Django Backend ────────────────────────────────────────────────────
  - type: web
    name: taskflow-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "taskflow-backend-cj9v.onrender.com,localhost,127.0.0.1"
      - key: DATABASE_URL
        fromDatabase:
          name: taskflow-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: taskflow-redis
          type: redis
          property: connectionString
      - key: CORS_ALLOWED_ORIGINS
        value: "https://taskflow-frontend-w25n.onrender.com"
      - key: EMAIL_BACKEND
        value: "django.core.mail.backends.smtp.EmailBackend"
      - key: EMAIL_HOST
        value: "smtp-relay.brevo.com"
      - key: EMAIL_PORT
        value: "2525"
      - key: EMAIL_USE_TLS
        value: "True"
      - key: EMAIL_HOST_USER
        sync: false   # Set this in Render Dashboard: your Brevo SMTP login (e.g. abc@smtp-brevo.com)
      - key: EMAIL_HOST_PASSWORD
        sync: false   # Set this in Render Dashboard: your Brevo SMTP key
      - key: DEFAULT_FROM_EMAIL
        value: "TaskFlow <shushantpes@gmail.com>"
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GITHUB_CLIENT_ID
        sync: false
      - key: GITHUB_CLIENT_SECRET
        sync: false
      - key: FRONTEND_URL
        value: "https://taskflow-frontend-w25n.onrender.com"

  # ─── React Frontend ────────────────────────────────────────────────────
  - type: web
    name: taskflow-frontend
    runtime: docker
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    plan: free
    envVars:
      - key: VITE_API_URL
        value: "https://taskflow-backend-cj9v.onrender.com/api"
        isDockerBuildArg: true
      - key: VITE_GOOGLE_CLIENT_ID
        sync: false
        isDockerBuildArg: true
      - key: VITE_GITHUB_CLIENT_ID
        sync: false
        isDockerBuildArg: true

  # ─── Redis ────────────────────────────────────────────────────────────
  - type: redis
    name: taskflow-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow all (Render internal only)

databases:
  # ─── PostgreSQL ───────────────────────────────────────────────────────
  - name: taskflow-db
    plan: free
    databaseName: taskflow_db
    user: taskflow_user
